<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1524995997946-a1c2e315a42f?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=400&q=60);">
  <div class="banner-container">
    <h1>Courses overview</h1>
    <p>Continue your course or begin a new one!</p>
  </div>
</div>

<div>
  <h1>MY COURSES</h1>
  <div>
    <% @my_courses.each do |course| %>
      <h1><%= course.name %></h1>
      <p><%= course.category %></p>
      <p><%= course.language %></p>
      <% if policy(Course).create? %>
        <%=  link_to "join course", subscription_path(course), method: :post %>
      <% end %>
    <% end %>
    <% if !@my_courses.present? %>
      <p>No courses yet!</p>
    <% end %>
  </div>
</div>

<div>
  <h1>ALL COURSES</h1>
  <div class="flags">
    <div class="searchbar">
      <div class="search-form-control form-group">
        <button class='button-flag' name="category" id="search_query">
          <%= link_to '🇬🇧', courses_path(language: "english")  %>
        </button>
      </div>
    </div>
    <div class="searchbar">
      <div class="search-form-control form-group">
        <button class='button-flag' name="sq_category" id="search_query">
          <%= link_to '🇳🇱', courses_path(language: "dutch")  %>
        </button>
      </div>
    </div>
    <div class="searchbar">
      <div class="search-form-control form-group">
        <button class='button-flag' name="sq_category" id="search_query">
          <%= link_to '🇫🇷', courses_path(language: "french")  %>
        </button>
      </div>
    </div>
    <div class="searchbar">
      <div class="search-form-control form-group">
        <button class='button-flag' name="sq_category" id="search_query">
          <%= link_to '🇮🇹', courses_path(language: "italian")  %>
        </button>
      </div>
    </div>
    <div class="searchbar">
      <div class="search-form-control form-group">
        <button class='button-flag' name="sq_category" id="search_query">
          <%= link_to '🇪🇸', courses_path(language: "spanish")  %>
        </button>
      </div>
    </div>
  </div>

  <div>
    <% @courses.each do |course| %>
    <h1><%= course.name %></h1>
    <p><%= course.category %></p>
    <p><%= course.language %></p>
      <% if policy(Course).create? %>
        <%=  link_to "join course", subscription_path(course), method: :post %>
      <% end %>
    <% end %>
  </div>
</div>


