<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- code below is the icon in the browswertab -->
    <link rel="icon" href="https://i.imgur.com/EQY8ogf.png">
    <link href="https://fonts.googleapis.com/css?family=Mukta&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP&display=swap" rel="stylesheet">
    <title>MAPit today, remember it tomorrow</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <div class="wrapper">
      <% if params[:controller] == 'pages' && params[:action] == "home" %>
        <%= render 'shared/navbar' %>
      <% else %>
        <%= render 'shared/navbar_coloured' %>
      <% end %>
      <%= render 'shared/flashes' %>
      <%= yield %>
      <%= javascript_include_tag 'application' %>
      <%= javascript_pack_tag 'application' %>
      <%= yield :after_js %>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>
